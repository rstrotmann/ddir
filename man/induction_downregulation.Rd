% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ind_tools.R
\name{induction_downregulation}
\alias{induction_downregulation}
\title{Flag concentration-dependent CYP mRNA down-regulation}
\usage{
induction_downregulation(x, fold_threshold = 0.5, by_donor = TRUE)
}
\arguments{
\item{x}{An \code{induction_experiment} object. A \code{FOLD} column is required.}

\item{fold_threshold}{Maximum fold-change at the highest concentration
that still counts as suppression, as numeric. Defaults to 0.5.}

\item{by_donor}{If \code{TRUE} (default), one row per isoform and donor. If
\code{FALSE}, donors are pooled and there is one row per isoform.}
}
\value{
A data frame with one row per isoform, or per isoform and
donor, and the columns:
\itemize{
\item \code{OBJECT} CYP isoform.
\item \code{DONOR} Hepatocyte donor (omitted if \code{by_donor = FALSE}).
\item \code{n} Number of concentrations used.
\item \code{min_fold}, \code{max_fold} Range of \code{FOLD}.
\item \code{fold_maxc} \code{FOLD} at the highest \code{CONC}.
\item \code{rho} Spearman correlation of \code{CONC} and \code{FOLD}.
\item \code{downregulation} \code{TRUE} if the curve meets both criteria above.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

For each CYP isoform (and, by default, each hepatocyte donor), decide
whether vehicle-normalized mRNA fold-change falls as precipitant
concentration rises, rather than being induced.
}
\details{
Only \code{SAMPLE == "test"} rows with \code{CONC > 0} and non-missing \code{FOLD} are
used. A curve is flagged when both of the following hold:
\itemize{
\item fold-change at the highest tested concentration is below
\code{fold_threshold} (default 0.5, i.e. ≤ 50\% of vehicle);
\item Spearman's rank correlation of \code{CONC} with \code{FOLD} is negative, so
fold-change tends to decrease as concentration increases.
}

\code{FOLD} is fold vs vehicle (1 = no change, < 1 = suppression). Percent
of positive control (\code{REL}) is not used. Isolated \code{FOLD < 1} points at
low concentration, which occur in ordinary induction curves, do not
trigger the flag.

The rank correlation uses order only, not a linear fit on the µM scale.
No p-value is required; typical induction experiments have too few
concentrations for a Spearman test to be informative.
}
\examples{
induction_downregulation(examplinib_in_vitro_ind)
induction_downregulation(examplinib_in_vitro_ind1)
}
